class Solution:    def findMinHeightTrees(self, n: int, edges: List[List[int]]) -> List[int]:        adj_list = get_adj_list(edges)        in_degrees = get_in_degrees(edges)        leaves = get_leaves(n, adj_list)        for leaf in leaves:            in_degrees[leaf] = 0        que = deque(leaves)        copy_que = []        while que:            copy_que = []            for _ in range(len(que)):                node = que.popleft()                copy_que.append(node)                for neighbor in adj_list[node]:                    in_degrees[neighbor] -= 1                    if in_degrees[neighbor] == 1:                        que.append(neighbor)        return copy_que   def get_in_degrees(edges):    in_degrees = defaultdict(int)    for node1, node2 in edges: